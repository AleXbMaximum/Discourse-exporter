# Screenshots

Real captures of Discourse Text Recorder **v4.0.0**, used by the top‑level [README](../../README.md).

| File | View |
|---|---|
| `01-capture.png` | Capture tab — API capture running on a large thread (live posts / images / time + shard preview) |
| `02-export.png` | Export tab — format picker, shard cap, export options |
| `03-settings.png` | Settings tab — theme, language, capture options, keyboard shortcuts |
| `04-activity.png` | Activity panel — capture + sharded‑ZIP export task cards (stages, progress, ETA) |

To refresh a shot, replace the file **keeping the same name** and the README picks it up automatically. Portrait PNGs at roughly the dock's native width (~400 px) look best.
